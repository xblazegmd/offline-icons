name: Build Geode Mod

on:
  push:
    branches:
      - "**"

jobs:
  build:
    strategy:
      fail-fast: false
      matrix:
        config:
        - name: Windows
          os: windows-latest

        - name: macOS
          os: macos-latest

        - name: iOS
          os: macos-latest
          target: iOS

        - name: Android32
          os: ubuntu-latest
          target: Android32

        - name: Android64
          os: ubuntu-latest
          target: Android64

    name: ${{ matrix.config.name }}
    runs-on: ${{ matrix.config.os }}

    steps:
      - uses: actions/checkout@v4

      - name: Build the mod
        uses: geode-sdk/build-geode-mod@main
        with:
          combine: true
          target: ${{ matrix.config.target }}

  package:
    name: Package builds
    runs-on: ubuntu-latest
    needs: ['build']

    steps:
      - uses: geode-sdk/build-geode-mod/combine@main
        id: build

      - uses: actions/upload-artifact@v4
        with:
          name: build-out
          path: ${{ steps.build.outputs.build-output }}

  release:
    if: contains(github.event.head_commit.message, '[release]')
    needs: ['build', 'package']
    runs-on: ubuntu-latest
    permissions:
      contents: write

    steps:
      - uses: actions/checkout@v4

      - name: Get Version/Changelog Stuff
        id: release_stuff
        run: |
          # Version
          MSG="${{ github.event.head_commit.message }}"
          VERSION=$(echo "$MSG" | sed "s/\[release\]//g" | sed "s/[[:space:]]*$//")
          echo "version=$VERSION" >> $GITHUB_OUTPUT

          # Changelog
          echo "changelog<<EOF" >> $GITHUB_OUTPUT
          python .github/format_changelog.py >> $GITHUB_OUTPUT
          echo "EOF" >> $GITHUB_OUTPUT

      - name: Create Tag
        run: |
          git config user.name "github-actions"
          git config user.email "github-actions@github.com"
          git tag ${{ steps.release_stuff.outputs.version }}
          git push origin ${{ steps.release_stuff.outputs.version }}

      - name: Download Artifact
        uses: actions/download-artifact@v4
        with:
          name: build-out
          path: build/

      - name: Create Release
        uses: softprops/action-gh-release@v2
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
        with:
          tag_name: ${{ steps.release_stuff.outputs.version }}
          name: ${{ steps.release_stuff.outputs.version }}
          draft: true
          files: build/*
          body: ${{ steps.release_stuff.outputs.changelog }}
